# Datenbankverbindung
spring.datasource.url=${database_test_url}
spring.datasource.username=${database_username}
spring.datasource.password=${database_password}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# timeout fuer eingeloggte benutzer
server.servlet.session.timeout=60m

# JPA und Hibernate (Datenbank-Steuerung)
# update: Vergleicht mein Java-Entity-Klassen mit der Datenbank.
# Gibt es neue Felder oder Tabellen, werden diese hinzugefuegt.
# Bestehende Daten bleiben erhalten. (Ideal fuer die Entwicklung).
# create: Loescht bei jedem Start alle Tabellen und legt sie komplett neu an.
# Alle Daten gehen verloren.
# create-drop: Wie create, loescht die Tabellen aber zusaetzlich auch wieder,
# wenn die Anwendung gestoppt wird.
# none: Es werden keine Aenderungen an der Datenbankstruktur vorgenommen.
# (Pflicht fuer den Live-Betrieb/Produktion).
spring.jpa.hibernate.ddl-auto=create-drop

# Logs anzeigen
logging.level.de.khudhurayaz.freshradar=DEBUG
logging.level.org.springframework.security=DEBUG

#Zeigt bei Fehlern einer Fehlerseite
spring.web.error.whitelabel.enabled=true
spring.web.error.path=/error